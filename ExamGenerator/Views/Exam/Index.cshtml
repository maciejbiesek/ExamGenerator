@model List<ExamGenerator.Models.EXAMS>

@{
    ViewBag.Title = "Index";
}

@section Scripts{
    @Scripts.Render("~/bundles/listScript")
}

<hgroup class="title">
    <h1>Dostępne egzaminy</h1>
</hgroup>

<div id="tables">
    <ul>
        <li>
            <span class="mainHeader">
                <span class="columnName">Nazwa</span>
                <span class="deleteOption">@Html.ActionLink("Generuj", "x", "x", routeValues: null, htmlAttributes: new { id = "registerLink" })</span>
            </span>
        </li>
        <li>
            <ul class="tabelRows">
                <li>
                    <span class="topHeader">
                        <span class="columnName">Nazwa</span>
                        <span class="deleteOption">Usuń</span>
                    </span>
                </li>
                @foreach (var exam in Model)
                {
                    <li>
                        <span class="columnName">@exam.Name</span>
                        <span class="deleteOption">@Html.ActionLink("X", "DeleteExam", "Exam", new { id = exam.Id }, new { @class = "delete-link" })</span>

                    </li>
                }
            </ul>
        </li>
    </ul>
</div>

<div id="delete-dialog" title="Usuwanie">
    <p>Jesteś pewien, że chcesz to usunąć?</p>
</div>